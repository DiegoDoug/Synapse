/**
 * Dashboard route page.
 * Placeholder shell — real content is built in Stage 1 Step 6 (Dashboard).
 */
export default function DashboardPage() {
  return <div className="p-6">Dashboard</div>;
}
